To build the Lean project, run the following command from the repository root: export PATH="$HOME/.elan/bin:$PATH" && cd lean_project && lake build


To set up the Lean development environment, run: curl https://elan.lean-lang.org/elan-init.sh -sSf | sh -s -- -y