# Index of Formal Verification and Analysis Documents

This project contains a comprehensive suite of documents detailing the formal verification, complexity analysis, and mathematical foundations of the `AbsSat` algorithm implemented in Lean 4.

## Core Formal Verification
- [formal_verification.md](./formal_verification.md): High-level overview of the formal verification goals and model.
- [formal_verification_join.md](./formal_verification_join.md): Mathematical proof of the soundness and completeness of the `Join` operation.
- [formal_compression_strategy.md](./formal_compression_strategy.md): Analysis of the compression strategy and the Structural Boundedness Theorem.
- [formal_bridge_owners_runpure.md](./formal_bridge_owners_runpure.md): Design proposal (Spanish) for the missing bridge theorem — reading via Owners ≡ paths of `run_pure` — with the full correctness chain, lemma decomposition, and attack order.
- [../docs/summary_formalization.md](../docs/summary_formalization.md): **Phase F5 complete and axiom-free** — formal verification of the `ReqFiltered` invariant for `GPathM` and Lemma L1 (0 `sorry`, 0 project axioms; the closure of `L1`/`L1_cor` is `[propext, Quot.sound]`, pinned by build-failing `#guard_msgs`). Architecture, proof strategy, the 2026-07-04 audit that found axioms A8/A9 false, the 2026-07-05 repair, and the theorem catalog.

### Bridge phases (executable ↔ pure mirror)
Phases F1–F6 and lemma L1 are complete and axiom-free (see the summary above). Phase F2 — including F2.c in both its fixpoint and per-node forms — lives in `AbsSat/GraphPath/Model/Fuel.lean`. Bridge lemma L2's soundness and narrowing directions live in `AbsSat/GraphPath/Model/Filter.lean`; L3's soundness direction in `Model/Up.lean`; L4's monotone direction and L6's join case in `Model/Join.lean`; L6's statement, seed case and executable falsifier (`lake exe l6search`) in `Model/L6.lean` and `Model/L6Search.lean`, and its `up` case reduced to the review step in `Model/L6Up.lean`. The *checker* for L6 is itself verified in `Model/Certificate.lean` (`isCert_sound`, `Supported_of_checkSupported`), so a clean `lake exe validate` run is a machine-checked `Supported g` for the graphs examined; and `Model/Verdict.lean` splits L6 into the half the SAT/UNSAT verdict consumes (`Inhabited`, equivalent to L6 at a *single* node) and the half the non-backtracking reader consumes (`Supported`, and in fact its preservation under the reading filter — `ReadStable`). The α-acyclicity route (Beeri–Fagin–Maier–Yannakakis instead of Cooper–Cohen–Jeavons) is measured and closed in `GraphMap/Hypergraph.lean` (`lake exe hyper`): the maps' constraint hypergraph is not α-acyclic. `Model/Extendable.lean` reduces L6 to backtrack-freeness by induction on the step index (`Supported_of_Extend`), and `Model/ExtendSearch.lean` (`lake exe extend`) refutes that hypothesis on real maps while finding no violation once the reader's own propagation (`filterAll`) is included — leaving propagation as the driver. `Model/PickInduction.lean` builds that induction: `Inhabited_of_pickValid` reduces `Inhabited` to `PickValid` (one-step `ReadStable`) plus a no-choice base case, with the descent's termination *proved* (`measure_filterAll_lt`) rather than assumed. `PickValid` is then ground down inside the same module: `isValid_filterRequire` discharges the pinning half, `isValid_review_of_pass` reduces the fuel loop to one pass, and `isValid_removeNode_of_other` reduces one pass to one node removal — leaving "no removal is the last global owner of its step". `Model/Certifies.lean` then states the design invariant where the author places it — at construction time, `Reachable g → isValid g → Inhabited g` — and discharges the `seed` and `join` cases of that induction, leaving `up` as the sole entry in the ledger. Restated in `ChainSound` currency it goes further: `CertifiesS_of_ReqChain` discharges every side condition of `ChainSound_upFiltering` (`MachineOk_reachable`, `nodes_below_of_pruned`) and reduces the whole invariant to `ReqChain` — the first statement in the ledger that mentions the requirements, and so the first one `GraphMap.MapReqs`'s 0/1/all result can attach to. That in turn weakens twice more: `FilteredChain` asks for the chain in the filtered graph rather than in `g` (strictly weaker), and since the filtered graphs are `review` fixpoints the obligation becomes `ArcImpliesChain` — a valid, arc-consistent fixpoint carries a complete co-owned chain — with every machine-specific hypothesis (`addNode`, `join`, seed, `MachineOk`, `Reachable`) discharged. What remains open is a single statement — `SupportedG g → SupportedG (review g)` — which is simultaneously L6's last case, L2's ⊇ direction and L3's ⊇ direction. Execution log: [../docs/plans/espejo_gpathm_lema_L1.md](../docs/plans/espejo_gpathm_lema_L1.md).

## Complexity and Performance
- [complexity.md](./complexity.md): Detailed asymptotic analysis of time and space complexity ($O(S^4)$).

## Narrative and Author Support (Spanish)
- [verificacion_narrativa_es.md](./verificacion_narrativa_es.md): Narrative explanation of the verification process in Spanish.
- [verification_inseguridad_autor_main.md](./verification_inseguridad_autor_main.md): Human-AI collaboration summary and publication recommendation.

### Sequential Reassurance Documents
- [v1: El Algoritmo es Sólido](./verificacion_inseguridad_autor.md)
- [v2: Matemáticas vs Incertidumbre](./verificacion_inseguridad_autor_v2.md)
- [v3: Join es una Unión Segura](./verificacion_inseguridad_autor_v3.md)
- [v4: La Batalla por la Complejidad](./verificacion_inseguridad_autor_v4.md)
- [v5: La Victoria Estructural](./verificacion_inseguridad_autor_v5.md)
- [v6: Reflexión Personal (Gemini)](./verificacion_inseguridad_autor_v6.md)
- [v7: Coherencia del O(S^4)](./verificacion_inseguridad_autor_v7.md)
- [v8: El Abogado del Diablo](./verificacion_inseguridad_autor_v8.md)
- [v9: De los Axiomas a los Teoremas (Claude)](./verificacion_inseguridad_autor_v9.md)
- [v10: Lo que las pruebas dicen sobre el algoritmo (Claude)](./verificacion_inseguridad_autor_v10.md) — qué son realmente los `owners`, las tres invariantes de una cadena y por qué los rangos de las pasadas de coherencia cargan con la demostración. **Su §1 está corregida por v11.**
- [v11: Dónde está de verdad el problema de Helly (Claude)](./verificacion_inseguridad_autor_v11.md) — corrige v10: L6 son dos enunciados (preservación y "sin zombis"), solo el segundo necesita la estructura 3SAT, y es el que sostiene la soundness de los veredictos.
- [v12: La construcción del mapa es la respuesta (Claude)](./verificacion_inseguridad_autor_v12.md) — cada requisito pinza un nodo y calla sobre lo demás: el mapa genera una red de restricciones **0/1/all**, clase en la que la consistencia de arcos decide. Mecanismo para la mitad abierta de L6, más dos defectos de `ImportCnf`. **Su ruta de tres pasos está corregida por v13.**
- [v13: La ruta de v12 no cierra (Claude)](./verificacion_inseguridad_autor_v13.md) — los tres pasos hechos, pero las tablas `owners` agregan sobre los vecinos y no heredan la forma 0/1/all; 164 contraejemplos. Qué sí quedó demostrado y qué diana queda.
- [v14: Lo que hay debajo de la vía (2) (Claude)](./verificacion_inseguridad_autor_v14.md) — el mapa es lineal y el estado de la máquina está acotado por una constante (`PathNodeId` guarda un nivel de historia): la abstracción exponencial funciona. Y por eso **"sin zombis", junto con una cota de tiempo, es P = NP**. Mediciones reproducibles con `lake exe width`. **Su §3 (la "señal de alarma") está retirada: era la cota `O(S⁴)` cumpliéndose.**
- [v15: Cómo comprobar la validez (Claude)](./verificacion_inseguridad_autor_v15.md) — `lake exe validate` comprueba "sin zombis" **directamente**, como invariante interno en cada estado, con búsqueda de cadena independiente del Reader. 161.839 nodos verificados, 0 zombis, incluidas cinco familias adversarias.
- [v16: El checker demostrado, y las dos mitades de L6 (Claude)](./verificacion_inseguridad_autor_v16.md) — ruta **C**: `Certificate.lean` demuestra que el *checker* de "sin zombis" es correcto, así que una ejecución limpia de `validate` **es `Supported g` demostrado** para ese grafo (la búsqueda sigue sin ser de fiar, a propósito). Ruta **D**: `Verdict.lean` separa las dos mitades de L6 — `Inhabited`, la que sostiene el veredicto, es `Supported` **en un solo nodo**. **Su §2 está corregida por el autor:** el Reader no retrocede, así que un zombi es una parada en seco que aborta la enumeración entera — `Supported` es precondición de *corrección* del lector, no de velocidad.
- [v17: La ruta E está cerrada (Claude)](./verificacion_inseguridad_autor_v17.md) — el hipergrafo de restricciones del mapa **no es α-acíclico**, así que Beeri–Fagin–Maier–Yannakakis no aplica: el ciclo es la estructura de variables compartidas entre cláusulas. Medible con `lake exe hyper`. Un resultado positivo habría *sido* la afirmación entera (los CSP α-acíclicos son polinómicos), así que el "no" no refuta nada: dice que la prueba debe venir de la **dinámica de la máquina**, no de la estructura del mapa.
- [v18: La ruta A, refutada — y lo que su refutación señala (Claude)](./verificacion_inseguridad_autor_v18.md) — `Supported_of_Extend` reduce L6 a *backtrack-freeness* por inducción sobre el paso, y `lake exe extend` **refuta la hipótesis**: 1.574 cadenas parciales muertas, incluida una completa de 0 a 12. Pero con **propagación** (`filterAll` tras cada selección, que es lo que hace el Reader) no se atasca ninguna. La co-posesión por pares no basta — lo que salva la propiedad es el filtro. Diana sucesora: `PickStable`/`Determined`.
- [v19: A′ — una obligación en vez de dos (Claude)](./verificacion_inseguridad_autor_v19.md) — `Inhabited_of_pickValid` reduce `Inhabited` a **una** obligación (`PickValid`) más un caso base sin elección. La **terminación del descenso está demostrada**, no supuesta: `measure` cuenta `gowners` y `filterRequire` tira justo los que discrepan. `lake exe extend --descend` ejecuta el descenso y verifica los endpoints con la ruta C: 16.444 estados en campaña aleatoria (150/150 limpias) más 310 en familias adversarias, **0 violaciones**, todos los endpoints con cadena certificada.
- [v20: `PickValid`, molida hasta una sola eliminación de nodo (Claude)](./verificacion_inseguridad_autor_v20.md) — el pinchazo es inofensivo (`isValid_filterRequire`, demostrado), el bucle de fuel se reduce a una pasada (`isValid_review_of_pass`, **sin axiomas**) y una pasada a una eliminación. Lo que queda: *ninguna eliminación de la review es el último owner global de su paso*. Incluye el límite de la ruta (`review` **tiene** que poder invalidar: así se reporta UNSAT) y la circularidad del atajo por cadenas, dicha explícitamente. **Su §6 está corregida por v21:** mezclé el régimen de construcción con el de lectura.
- [v21: El invariante va en la construcción (Claude)](./verificacion_inseguridad_autor_v21.md) — corrección del autor: UNSAT se decide en `upFiltering`, no en la lectura, así que una invalidación durante el Reader sería una violación del invariante, no un veredicto. `Certifies.lean` enuncia la afirmación donde va — `Reachable g → isValid g → Inhabited g` — y hace la inducción: **`seed` y `join` demostrados**, queda solo `up`. El caso `join` sale porque `okJoin` ya exige válidas las dos ramas.
- [v22: `UpCertifies` reducido a un enunciado sobre los requisitos (Claude)](./verificacion_inseguridad_autor_v22.md) — reenunciado el invariante en moneda `ChainSound`, todas las condiciones laterales de `ChainSound_upFiltering` quedan demostradas (incluida `MachineOk_reachable`) y el ledger se cierra hasta **`ReqChain`**: *si el filtro de los requisitos deja el grafo válido, el grafo tiene una cadena sonora que pasa por esos requisitos*. Es el primer enunciado del ledger que menciona los **requisitos** — y por tanto el primero al que `MapReqs.Functional` (0/1/all) puede conectarse.
- [v23: La obligación final, dicha en la moneda clásica (Claude)](./verificacion_inseguridad_autor_v23.md) — `ReqChain` se debilita a `FilteredChain` (la cadena hace falta en el grafo **filtrado**, no en `g`), todo colapsa a `ValidHasChain`, y como los filtrados son **puntos fijos de `review`** el enunciado se reformula como **`ArcImpliesChain`**: *un punto fijo válido y arco-consistente lleva una cadena completa co-poseída*. Ya no queda dentro nada de `addNode`, `join`, la semilla, `MachineOk` ni `Reachable`. **Refutado por v24:** quitar `Reachable` fortalece la obligación, y la vuelve falsa.
- [v24: `ArcImpliesChain` es falso (Claude)](./verificacion_inseguridad_autor_v24.md) — testigo explícito: un grafo con un owner global en cada paso y **ningún nodo** es válido, es punto fijo de `review` y es arco-consistente por vacuidad, pero no tiene cadena. `not_ArcImpliesChain` demostrado. La reparación (`ArcImpliesChainOn`) resulta **equivalente** a `FilteredChain`, así que la escalera de reducciones tocó fondo ahí. El contraejemplo nombra el invariante que faltaba: **`GownersAreNodes`** — todo owner global es un nodo — que `Reachable` suministraba en silencio.
- [v25: `GownersAreNodes`, demostrado para toda la máquina (Claude)](./verificacion_inseguridad_autor_v25.md) — corrige v24: `filterRequire` **no** rompe el invariante (encoge `gowners` y deja los nodos; lo que rompe es la recíproca). Con la dirección correcta se demuestra operación por operación —`filterRequire`, `updateAt`, `removeNode`, toda la cadena de `review`, `addNode`, `join`, `initSeed`— y de ahí `GownersAreNodes_reachable`. Corolario `node_at_every_step`: la validez deja de decir *hay un owner en cada paso* y pasa a decir **hay un nodo en cada paso**, que es lo que a `degenerate` le faltaba.
- [v26: Los dominios no están vacíos — lo que queda es la elección (Claude)](./verificacion_inseguridad_autor_v26.md) — `owner_is_node`: **todo owner de un nodo superviviente es él mismo un nodo**, encadenando `owners_mem_gowners` con v25. De ahí `candidate_at_step`: el soporte de cualquier nodo en cualquier paso es no vacío **y está hecho de nodos**. Separa `FilteredChain` en "¿hay candidatos?" (demostrado) y "¿se pueden elegir a la vez?" (abierto), y deja gratis la cláusula `gowners` de `ChainG`.
- [v27: `IsChain` demostrado — el camino existe (Claude)](./verificacion_inseguridad_autor_v27.md) — `exists_isChain`: descenso desde un nodo del paso más alto bajando por los padres hasta el paso 0. Necesitó tres invariantes nuevos (`Model/Parents.lean`): **`PN`** (todo padre es un nodo superviviente), **`PBelow`** (un padre está un paso por debajo) y **`NotRoot`** (nada por encima del paso 0 es raíz, vía `MachineOk`). `PairwiseOwned` queda **completamente aislado** — el camino construido no tiene por qué estar co-poseído, y ahí está la Helly.
- [v28: La vía barata a `PairwiseOwned` está cerrada (Claude)](./verificacion_inseguridad_autor_v28.md) — medida la relación de posesión sobre 259.187 nodos: **`nodes ⊆ gowners` y auto-posesión, 0 violaciones**; **simetría refutada** (1.364 violaciones) y **clique del soporte refutado** (63,9 M). `SupportClique_gives_PairwiseOwned` demuestra que el clique habría cerrado `ChainSound` junto con v27, así que la refutación tiene coste medido. Y es **la Helly en el vocabulario de la máquina**: dos nodos pueden ser compatibles con `t` e incompatibles entre sí. `self_owned_of_SelfOwned` deja pagada una de las condiciones que faltaban.
- [v29: `son_link` y `root_shape` cerrados (Claude)](./verificacion_inseguridad_autor_v29.md) — `SMP` (las tablas de padres e hijos se reflejan) demostrado para toda la máquina, `join` incluido; y `RootAtZero`, espejo de `NotRoot`, otra vez vía `MachineOk`. Con eso `son_link_of_SMP` y `root_shape_of`. **`self_owned` resulta ser de otra clase:** habla de `owners`, que las pasadas podan, y es **mutuamente recursivo con `ParentOwns`** — hace falta inducción simultánea con `NodesAreGowners`. Cuatro de los seis requisitos de `ChainSound`, cerrados.
- [v30: `self_owned` cerrado — y `ParentOwns` no hacía falta (Claude)](./verificacion_inseguridad_autor_v30.md) — la recursión mutua de v29 era real pero era la ruta equivocada. **`OOS`**: en su propio paso, los owners de un nodo no contienen nada más que a él — cierto desde `addNode` (el nodo nuevo recibe `gowners ++ [pid]` con todos los gowners por debajo) y estable porque solo encoge. Entonces la auto-posesión es **consecuencia** de `isValidNode` en el punto fijo, no un invariante que preservar. **Cinco de los seis requisitos de `ChainSound`; queda `PairwiseOwned`.**
- [v31: La estructura del mapa entra por los requisitos (Claude)](./verificacion_inseguridad_autor_v31.md) — un requisito pinza un nodo en un paso (`MapReqs.Functional`), así que la condición natural sobre una cadena es **satisfacer los requisitos**, no la co-posesión. `reqSatisfying_of_pairwiseOwned` demuestra una dirección (L1 leído a lo largo de la cadena); la recíproca, `ReqSatImpliesOwned`, **sobrevive a la medición**: **350.243** caminos que satisfacen los requisitos en dos campañas independientes (160 instancias, 19.327 estados), **0 no co-poseídos**. Y `ChainSound_of_parts` ensambla todo lo demostrado: `ChainSound` sale de **dos** enunciados abiertos, uno de los cuales es *que la fórmula sea satisfacible*. **Su §5 está corregida por v32.**
- [v32: El mapa dibuja, la máquina garantiza (Claude)](./verificacion_inseguridad_autor_v32.md) — corrección del autor: el mapa **dibuja** la expresión 3SAT, y el dibujo **no garantiza** que exista un camino que la satisfaga; es la máquina la que, tras procesarlo, si obtiene un conjunto válido garantiza la satisfacibilidad, y si no puede construirlo paso a paso eso **es** la detección de UNSAT. La obligación correcta es sobre los **estados válidos que la máquina sostiene**, no sobre el mapa — y así planteada **se puede medir**: **19.327** estados válidos en dos campañas independientes, **0 sin camino que satisfaga sus requisitos**.
- [v33: El hueco de la co-posesión es de factor dos (Claude)](./verificacion_inseguridad_autor_v33.md) — `ReqSatisfying` habla de **ids de mapa** y `PairwiseOwned` de **`PathNodeId`s**; ahí está el hueco. `owner_at_req_shares_mapid` demuestra que en un paso pinzado *todos* los owners coinciden con la elección de la cadena en el id de mapa, así que solo puede diferir el padre. Y la medición lo acota: sobre **1.654.908** pares (nodo, requisito) en dos campañas, el conjunto de owners más ancho en un paso pinzado es **2**, nunca tres.
- [v34: `parent_id` no era decoración (Claude)](./verificacion_inseguridad_autor_v34.md) — dos invariantes nuevos (`TL`: la línea de arriba lleva `map_parent`; `PMP`: todo padre lleva el id de mapa que nombra `parent_id`) dan la condición local `parentId_coherent`, y de ahí **`chain_eq_of_mapIds_eq`: una cadena está determinada por su secuencia de ids de mapa**. Eso **deshace el factor dos de v33**: en un paso pinzado el path-node de la cadena no es "uno de dos", está fijado; lo que falta es solo que el fijado esté en la lista de owners. **Su §4 está corregida por v35.**
- [v35: El lema propuesto en la revisión externa es falso (Claude)](./verificacion_inseguridad_autor_v35.md) — `∀ q ∈ owners, q = sel req.step` es **falso**: 1.035.280 de **4.429.212** pares (nodo de cadena, requisito) tienen conjuntos de owners de ancho 2. Lo que `PairwiseOwned` necesita es **pertenencia**. Y la pregunta empírica tiene respuesta perfecta: **siempre exactamente un** owner casa con el predecesor de la cadena, nunca cero ni dos, en **4,43 M** de casos y dos semillas. De ahí `owner_eq_chain_pick` y `chain_pick_mem_owners`: la mitad pinzada se reduce a **una existencia**.
- [v36: El puente entre los dos libros (Claude)](./verificacion_inseguridad_autor_v36.md) — el puente mínimo (**todo padre es owner**) es **falso**: 37 de 328.086 enlaces padre y 71 de 328.086 enlaces hijo faltan de los owners, porque **la poda de owners nunca desenlaza un padre** y la tabla `parents` se queda obsoleta. Pero es cierto **exactamente donde hace falta**: sobre pares consecutivos de caminos req-satisfactorios, **3.556.470** pares en dos campañas y **0 excepciones**. El puente no es estructural — sigue necesitando el lado del mapa.
- [v37: El bug corregido en el ejecutable (Claude)](./verificacion_inseguridad_autor_v37.md) — el autor identifica el fallo: **la poda de owners debe desenlazar a los vecinos incompatibles**, y un nodo sin padres se invalida y se elimina. Corregido en `GraphPath.lean` (`unlink_incompatible!`, desenlace simétrico) y validado: **`diffTest` 800/800**, veredictos y conjuntos de soluciones sin cambio. El espejo queda pendiente de migrar.
- [v38: El puente, formalizado (Claude)](./verificacion_inseguridad_autor_v38.md) — `LinksInOwners` pasa de **refutado** (v36) a **cierto por construcción**: `linksInOwners_at` demuestra que el arreglo lo establece, `pruned_unlinkIncompatible` que es un estrechamiento legítimo, y `chain_link_survives` que el desenlace no puede romper una cadena sonora **porque está co-poseída**. Lo que falta para el invariante global es cablear la operación en el espejo; estado de la migración documentado.
- [v39: El puente es un teorema — y el espejo lleva el arreglo (Claude)](./verificacion_inseguridad_autor_v39.md) — `linksInOwners_review` demuestra **`LinksInOwners` en todo punto fijo válido de `review`** (y por tanto en `filterAll`): todo padre y todo hijo es owner. La ruta es corta porque el arreglo del autor hace el trabajo — el puente no se demuestra, se observa. Migración completa al espejo (`unlinkIncompatible` cableado en `cleanInvalidGo` y `reviewNode`, ocho módulos reparados). Medido **con las mismas banderas antes y después**: 5/2 enlaces fuera de owners → **0/0**, y los **277** padres rancios de la campaña de v36 → **0**, con los mismos 2.563.751 pares de cadena. Retira el aviso de v36 sobre `exists_isChain`.

- [v40: `PairwiseOwned`, cerrado donde la inducción lo necesita (Claude)](./verificacion_inseguridad_autor_v40.md) — la vía que abría el puente está **refutada** (la posesión no es transitiva: 15.240 de 420.078 tripletas bajando, 18.919 de 416.403 subiendo). La que sí funciona no propaga la relación, **quita la elección**: `pid_unique` demuestra que en un estado totalmente pinzado hay **un solo nodo por paso** — el id de mapa lo clava el filtro, el `parent_id` lo clava el paso de abajo — y entonces `pairwiseOwned_of_fullyPinned` sale solo. Y ese régimen es **literalmente** el `NoChoice` de v19, así que **el caso base de la ruta A′ queda descargado** y `Inhabited_of_pickValid` se queda con una sola obligación, `PickValid`. Medido: 3.969 estados pinzados reales en dos semillas, **paso más ancho = 1** en todos.

- [v41: `PickValid` — más pequeño, localizado, y con media pieza nueva (Claude)](./verificacion_inseguridad_autor_v41.md) — **no cerrado**, pero tres avances concretos. (i) La obligación se debilita a **`PickSome`** (∃ una elección buena por etapa, no ∀), con `Inhabited_of_pickSome` demostrado; medido, la forma ∀ también aguanta: **0 de 63.314** elecciones invalidan. (ii) El riesgo está **localizado**: `cleanInvalid` —la pasada que hace el pinzado— nunca invalida (0 de 39.984) y las de coherencia solo quitan algo en 67 elecciones. (iii) **`threaded_below`**: todo nodo tiene un camino desde el paso 0 **cuyos nodos lo poseen todos** — estrictamente entre v27 y `PairwiseOwned`, y pagado por `coherent_parents`. Lo que falta por encima del ancla es un invariante nombrado, **`SAbove`** (todo hijo un paso arriba), que `Pruned` no puede dar porque no lleva cláusula de hijos.

- [v42: `SAbove` demostrado — y con él, el enhebrado completo (Claude)](./verificacion_inseguridad_autor_v42.md) — el invariante que v41 dejó nombrado es teorema: **todo hijo está un paso por encima**, en toda la máquina. Salió barato porque `SAbove` es *local*: un solo lema (`SonsSub` — los ids no cambian y los hijos solo encogen) cubre toda la poda, y solo `addNode` tiene algo que decir. Con él, **`threaded`**: todo nodo por encima del paso 0 está en un camino completo del paso 0 a la cima **cuyos nodos lo poseen todos** — se sube con `coherent_sons`+`SAbove` (solo existencia) y se baja con `coherent_parents` (que da los enlaces). Queda fuera el **paso 0**, porque `reviewSons` no lo barre: medido, **3.090 de 63.314** elecciones (4,9 %).

- [v43: la supervivencia a `cleanInvalid`, demostrada — y el testigo no es un camino (Claude)](./verificacion_inseguridad_autor_v43.md) — el hallazgo primero: **un camino que se sostiene a sí mismo *es* `PairwiseOwned`**, así que ningún ataque por caminos (v27, v41) puede cerrar esto; pero `isValid` no pide una cadena, pide un owner por paso. Con el objeto correcto —un **conjunto auto-sostenido**, `Closed`— sale el teorema: **la barrida no puede eliminar a un miembro**, y si el conjunto cubre los pasos, `cleanInvalid` deja el grafo válido. De sus seis cláusulas, `coown` queda demostrada (`coown_of_bridge`, cierre `[propext]`), tres son gratis, una es un hueco estructural (el espejo hijos→padres) y una es **el residuo**: *en cada paso, un miembro posee a un miembro* — que es `PickValid` sin cadenas. Medido: **0 fallos en 5,0 M** de comprobaciones, dos semillas.

- [v44: `support` no se demuestra — se quita de la cuenta (Claude)](./verificacion_inseguridad_autor_v44.md) — las seis cláusulas de `Closed` son cerradas bajo unión (porque `coown` no depende del conjunto), así que **el mayor conjunto auto-sostenido las cumple por construcción**: `Closed_Core`. Con eso la obligación pasa de seis cláusulas a **un solo enunciado**, `CoreCovers`: *el núcleo llega a todos los pasos*. Sin cadenas, sin `isValidNode`, sin bucle de fuel. Medido **sin circularidad** (el núcleo se calcula con las cláusulas de `Closed`, nunca con `isValidNode`): **24.877 pinchazos, 0 pasos vacíos**. Y el mapa del muro desde tres lados: `PairwiseOwned` (la cadena), `support` (el camino), `share` (las pasadas de coherencia) — el mismo enunciado tres veces.

- [v45: `CoreCovers` partido en dos — y una mitad es teorema (Claude)](./verificacion_inseguridad_autor_v45.md) — el núcleo se obtiene **estrechando** un candidato, y **que el candidato cubra todos los pasos es teorema** (`pinSet_covers`, vía el enhebrado de v42): la dificultad no es si hay algo compatible con la elección, sino si la compatibilidad sobrevive a su propia clausura. Cadena completa demostrada del pinchazo a `isValid (cleanInvalid …)` con solo dos hipótesis. Y **`support` es gratis en cuatro pasos** —el pinzado, el propio y los dos vecinos— porque el puente de v39 hace owner a padres e hijos; el residuo es `support` **a distancia ≥ 2**, justo donde la transitividad refutada de v40 lo habría llevado. Medido: 3.473.942 comprobaciones lejanas, **0 fallos** (un 20 % de las de v43 pasó a ser teorema).

- [v46: el residuo, acorralado por tres lados (Claude)](./verificacion_inseguridad_autor_v46.md) — dos refutaciones y una caracterización. **La versión fuerte es falsa**: no todo owner de un candidato es candidato (722.851 de 3,7 M; 1,4 M de 6,3 M en otra semilla), así que el ∃ de `support` es esencial. **El testigo obvio tampoco vale**: el descenso anclado al pinchazo se sale del soporte del nodo de partida en 6.371 de 125.528 descensos. Y lo que sí es teorema, `owner_below_on_descent`: **todo owner por debajo de un nodo está en un descenso desde él**, con el nodo en el paso del owner siendo el owner mismo (`OOS`) — lo que encierra la tabla de owners entre los padres (el puente) y los ancestros, sin que ninguna inclusión sea igualdad. El residuo queda **cierto, estrictamente existencial y sin construcción**: lo que falta es una *regla de elección*.

- [v47: el espejo inverso, y con él dos huecos cerrados (Claude)](./verificacion_inseguridad_autor_v47.md) — **`SN`** (los hijos son nodos) y **`PMS`** (si `s` es hijo de `n`, `n` es padre de `s`), demostrados para toda la máquina. Toda la mitad de poda de `PMS` —`unlinkIncompatible` incluido— es el **calco literal** de `SMP` con `parents`/`sons` intercambiados y compiló entera sin tocar una línea: eso es tu arreglo del bug, que hizo el desenlace simétrico. Con `PMS` caen los dos agujeros que dependían de él: **`threaded` pierde la hipótesis `1 ≤ a.id.step`** (todo nodo, sin excepciones, está en un camino completo que lo posee entero) y **la cláusula `son` deja de ser hipótesis por encima del paso 0**.

- [v48: el paso 0 y la barrida de hijos — estudio (Claude)](./verificacion_inseguridad_autor_v48.md) — respuesta a una pregunta del autor. De las cuatro cotas de las dos pasadas de coherencia **tres están forzadas y una no**: un nodo del paso 0 sí tiene hijos, así que la cota inferior de `review_owners_sons_parents!` no tiene razón. **No es un fallo de corrección** (la omisión solo agranda los owners, nunca invalida: 0 de 3.292 y 0 de 7.333) pero **no es un no-op**: deja soporte rancio en el 0,1–0,7 % de los nodos raíz. Aplicado y reconstruido, rompe **tres anotaciones de rango y ninguna demostración** — porque `ChainSound_reviewLine_sons` ya estaba demostrado con `0 ≤ k`. Gana: cierra el hueco de v47 (`Closed.son` en el paso 0) y hace simétricas las dos cláusulas de arco-consistencia. No gana: el residuo `support` no se mueve. `diffTest` 400/400 con el espejo extendido. **Aplicado** a ejecutable y espejo por decisión del autor: `diffTest` 400/400 y 300/300, `validate` 50/50, y la medición que lo detectó pasa a **0** con el mismo recuento de estados. `Closed_PinSet` baja de dos hipótesis a **una**: solo `support`.

- [v49: recap del estado de validación, y qué falta para cerrarla (Claude)](./verificacion_inseguridad_autor_v49.md) — informe de estado auditado contra el árbol. La corrección de encuadre: **la validación completa tiene cinco eslabones, no uno** — CNF→GMap (sin verificar), GMap→estados (demostrado), estado→cadena (abierto, el problema), cadena→asignación (sin verificar) y ejecutable↔espejo (empírico). Inventario de lo demostrado, lo medido y lo refutado, el residuo con sus cinco caras, y los siguientes pasos por orden de rentabilidad — con **el puente con 3SAT (eslabones 1 y 4) como lo más rentable, porque no depende del problema abierto**.

- [v50: la ley de conservación — el veredicto, cerrado por los dos lados (Claude)](./verificacion_inseguridad_autor_v50.md) — corrección de estrategia, a partir de una observación del autor: *el conjunto válido contiene no una solución sino todas*. Eso no es una existencia sino una **ley de conservación**, y se demuestra por inducción sobre la construcción. **`chainSound_along`**: para toda asignación que satisface φ, la selección que nombra es cadena sonora en todo estado de su rama — **el testigo viene de fuera**, así que la máquina no tiene que construir nada, solo no destruirlo. Los cuatro pasos de la inducción ya estaban demostrados; la hipótesis que faltaba era `reqSat_selOfAssign`, de M3. Y sale gratis lo mejor: **la máquina no puede invalidar un estado que todavía contiene una solución** — la validez pasa de obligación a consecuencia. Con `L7.sat_of_inhabited`, `sound_and_complete`: que el conjunto sea no vacío **es** la satisfacibilidad.

- [v51: la rama es un camino del mapa, y llega hasta el final (Claude)](./verificacion_inseguridad_autor_v51.md) — la pieza que v50 dejó pendiente. **`selOfAssign_son`**: la secuencia que nombra una asignación no es una sucesión elegida a mano, es un **camino a lo largo de las aristas del propio mapa** — y el caso bonito es que el enlace cruzado de `add_var!` (`"v=0"`→`"!v=1"`) y la negación de la asignación **son la misma operación**. Con **`exists_full_valid_state`**: una fórmula satisfacible da un estado que las operaciones de la máquina alcanzan, abarca el mapa entero, sigue válido y denota una solución — y la validez que la recursión necesita en cada paso **la suministra la propia ley de conservación**. La banda `cnfmap` compara ahora también los **hijos**: 40/40 y 120/120, 0 desacuerdos.

- [v52: el driver, puro y validado (Claude)](./verificacion_inseguridad_autor_v52.md) — `mirrorRun` vive sobre `GMap`/`HashMap`, así que se aplica una vez más el patrón del proyecto: **`pureRun`**, el mismo bucle sobre el modelo aritmético, más una banda que lo ata al real — **190 instancias, 0 desacuerdos** en claves finales, recuentos de nodos y validez. Y el teorema que es el contenido: **`advance_target`** — desde el estado de la rama en el paso `k`, el hijo al que el driver va a moverse **es** el nodo de la asignación en `k+1`, el estado que construye allí es el siguiente de la rama, y **pasa el filtro de validez**. Lo que queda es contabilidad sobre la lista (tres invariantes, nombrados), no matemáticas.


- [v53: el driver conserva la rama — teorema (Claude)](./verificacion_inseguridad_autor_v53.md) — la contabilidad que v52 dejó nombrada, demostrada. **`pureRun_full_state`**: para *toda* asignación que satisface φ, la última línea del driver tiene una entrada **en el nodo que esa asignación nombra**, y el estado aparcado ahí abarca el mapa entero, es válido y denota algo — el cuantificador es el que tú decías, no una solución sino **todas**. El lema que lo sostiene es `Carries_insertPure`: ninguna de las dos cosas que el driver sabe hacer con una entrada puede perderla — insertar en otra clave no la toca, fusionar sobre ella solo la hace crecer. Con `init_ok` y `run_ok`, el bucle entero; con `pureRun_ne_nil`, la línea final no vacía siempre que φ sea satisfacible. Cierre `[propext, Quot.sound]`. Bandas tras el refactor: `--driver` 130/130, `cnfmap` 200/200, `diffTest` 300/300. **La mitad de completitud queda cerrada de punta a punta, de la fórmula al bucle**; el muro se reduce a un solo eslabón por un solo lado: leer una solución de un estado válido *arbitrario*.

- [v54: la ruta C, atacada — el residuo se reduce al descenso (Claude)](./verificacion_inseguridad_autor_v54.md) — medir antes de demostrar, y las mediciones dijeron más de lo esperado. **La simetría de la propiedad es falsa** (185 de 116.330), y falla *exactamente* donde vive el residuo: todas a distancia ≥ 2, ninguna en un par enlazado, y otro `review` no elimina ni una — así que la vía «voltear `threaded`» queda cerrada antes de invertir en ella. La cadena golosa de `threaded` no siempre es co-poseída (891 fallos), pero la búsqueda encuentra una **siempre**: **0 zombis en 55.838 anclas**, presupuesto nunca agotado — la primera medición directa de `SupportedAt` sobre mapas de CNF reales. Y el hallazgo: goloso **con historia** baja los fallos de 891 a **7**, y **bajando no se atasca jamás (0 de 55.838)**; los siete son todos subiendo, lo que encaja con que la máquina construya hacia arriba (el pasado está podado, el futuro no). Como `Inhabited` solo necesita **un** nodo, se elige uno del paso alto y se desciende: **3.719/3.719**. El objetivo pasa de `Supported` (todo nodo, ambas direcciones) a **`DownH`**: *un nodo tiene un padre compatible con toda la historia* — local, de un paso, universal, una sola dirección, y con la forma que `hop_down` ya tiene demostrada.

- [v55: el veredicto, con la mitad refutada fuera (Claude)](./verificacion_inseguridad_autor_v55.md) — el reparo que había que hacer antes de escribir Lean: `ExtendDown` general también estaba refutado (57 callejones bajando), así que la medición golosa de v54 no bastaba. Pero la demostración compuesta solo aplica el descenso a cadenas **que ya llegan al paso alto** — un subconjunto estricto. Ese es **`ExtendDownTop`**, y medido *exhaustivamente* (todas las cadenas, no la golosa) da **0 callejones en 15.755 cadenas** desde 5.393 anclas altas, cuatro semillas, ninguna búsqueda cortada. Y la reducción es teorema: **`Inhabited_of_ExtendDownTop`** — `isValid` pone un owner en el paso alto, `GN` lo hace nodo, el descenso lo enhebra, y `Verdict.Inhabited_of_SupportedAt` lo convierte en denotación — **sin que `ExtendUp` aparezca**. El residuo queda desnudo: `hop_down` da *para cada* owner *algún* padre que lo posee; falta *algún* padre que los posea *todos*. Y el descenso resulta casi determinista (menos de tres cadenas por ancla), porque los padres de un nodo comparten el id de mapa que `parent_id` codifica.

- [v56: el ∃∀ sí está al alcance — y el residuo es ahora local (Claude)](./verificacion_inseguridad_autor_v56.md) — la respuesta a la pregunta de v55 tiene dos mitades. **El id de mapa sí está determinado**: los padres de un nodo nunca abarcan dos ids (0 de 26.878), así que `parent_id` decide el nodo de mapa del padre — siempre. **Pero la posesión no**: hermanos con el mismo id de mapa tienen owners distintos en 5.182 pares, así que el atajo se cae. Y el ∃∀ a lo bruto es **falso** (948 de 26.878 nodos no tienen padre bueno para *todos* sus owners de arriba) — porque se le pedía cubrir conjuntos que **ninguna cadena podría contener**. Restringido a exigencias coherentes (una *transversal clique*: como mucho un nodo por paso, mutuamente poseídos) y enumerando **todas** las sub-cliques: **0 fallos en 154.635.941 exigencias**. De ahí **`GoodParentOnCliques`** y la cadena entera demostrada hasta `Inhabited`. El residuo pasa a ser **local, de un paso, universal y sin cadenas** — la primera vez en todo el desarrollo que no habla de caminos.

- [v57: el ataque a `GoodParentOnCliques` — tres atajos caídos, dos teoremas (Claude)](./verificacion_inseguridad_autor_v57.md) — fui a por la demostración y **no cayó**; lo que salió son tres atajos descartados con números y dos teoremas. **Los padres hermanos son siempre incomparables** (4.274/4.274 y 4.980/4.980), así que no hay padre máximo. **La mitad *arriba* no se sigue de la de *abajo***: 0 fallos con la primera semilla, **38 de 594.332 con la segunda** — el atajo habría pasado el primer filtro. Pero los 38 están **todos en nodos que ramifican**: sin ramificación, 0 de 473.730. De ahí **`owners_subset_of_unique_parent`** (con un solo padre, `owners p ⊆ owners c`, el conjunto entero — la mitad *abajo* demostrada en el 80 % de las configuraciones) y, viendo que un nodo tiene como mucho 4 padres y que ≥3 es el 0,1 %, **`good_of_pairwise_two`**: con dos padres, bueno para cada **par** ⟹ bueno para toda la exigencia. El enunciado abierto pasa de «para toda exigencia» a «para cada par de owners», salvo un 0,1 % de nodos donde hace falta Helly de verdad.

- [v58: el lector, tal y como lo diseñaste — ensamblado (Claude)](./verificacion_inseguridad_autor_v58.md) — corrección del autor, atendida. Leído `PathReader` y `filter!` en el original: seleccionar, **pinar el id de mapa** (`filter_require!` = «asumimos que hemos seleccionado todos los owners»), **revisar hasta el punto fijo** (`make_review_owners!` es recursivo), abortar si deja de ser válido, repetir — y terminar con **un nodo de mapa por paso**, que es una solución. Ese proceso **ya era** `Inhabited_of_pickSome` + `Pinned.inhabited_of_noChoice`; lo que faltaba era la clase cerrada bajo el pinado **repetido**, porque todos los invariantes estaban enunciados para *un* `filterAll` sobre un estado `Reachable`. Tres piezas nuevas: **`NodeIds.lean`** (la lista de ids solo encoge — map preserva ids, filter recorta — luego **`NodupIds_filterAll`**, la hipótesis que nadie descargaba), **`Reader.lean`** (`RCtx` con sus siete invariantes y su transferencia, `Readable`, el contexto y la cadena sobre la clase) y **`NodupIds_reachable`** (semilla/`up`/`join`). Resultado: **`Inhabited_of_pickSome_machine`** — el bucle del lector, con **una sola hipótesis**, `PickSome`, que es justo el `throw("GRAVE ERROR READER")` de tu código. Medida: `--read` 40/40, `PickSome` 0/2.759 estados, `PickValid` 0/64.144 selecciones.

- [v59: las pasadas de coherencia, cubiertas — y el lector sigue el camino (Claude)](./verificacion_inseguridad_autor_v59.md) — cae el hueco que v44 dejó marcado. Aquel informe ya había identificado el **punto fijo** de la cláusula `share` que las pasadas de coherencia necesitan —«una selección poseída por todos los miembros, y como cada una es miembro, se poseen entre sí: eso es `PairwiseOwned`»— pero la perseguía cadena abajo en vez de pedirla directamente. **`Woven` = `Closed` + los miembros se poseen entre sí**: es `share` en una línea, y **se mantiene sola**, porque las pasadas solo intersecan con listas que ya contienen a todos los miembros. Con dos piezas generalizadas (`Closed_updateAt_of`, `isValidNode_of_Closed_of`) sale la cadena entera hasta **`isValid_filterAll_of_Woven`**: un conjunto woven que cubre mantiene el pinchazo válido *con las dos pasadas incluidas*. Y **una cadena co-poseída es woven** —cada cláusula es una de `IsChain` o `PairwiseOwned`, y la de posesión mutua **es** `PairwiseOwned`— así que **`isValid_pin_of_chain`**: *el lector nunca destruye la solución que está siguiendo*. Con v58 en la otra dirección, **`PickSome` ⟺ `Inhabited`**: el `throw("GRAVE ERROR READER")` no es un apoyo más débil que «sin zombis», es el mismo enunciado — demostrado, no sospechado. El muro queda solo: **existe una cadena co-poseída en todo estado válido**.

- [v60: tu frase, medida — y por qué la inducción nunca podía funcionar (Claude)](./verificacion_inseguridad_autor_v60.md) — la conjetura, medida **sin pasar por el grafo**: enumerar las 2ⁿ asignaciones y preguntar cuáles siguen dentro del estado. En el **estado final**: **0 de 115** estados válidos sin solución dentro, y algo más fuerte que no se pedía — **0 de 5.681 owners espurios**: los nodos de mapa que sobreviven son *exactamente* los que usan las soluciones supervivientes. Φ no contiene «al menos una» solución, **contiene el conjunto de soluciones**. Pero la misma cuenta en estados **intermedios que aún contienen solución** da **8,9 %–10,3 % de espurios**: la exactitud **no es un invariante que se pueda arrastrar**, solo se vuelve cierta al completar el recorrido. Y eso explica de una vez el fracaso repetido de v12, de `Inhabited_of_descent`, de la ruta A y de toda la línea v54–v57: **buscaban un invariante que no existe a mitad de camino**. La demostración no puede ser una inducción sobre la construcción; tiene que ser un argumento de **punto fijo** sobre el estado terminado. El muro pasa a tener nombre semántico: **todo owner superviviente es usado por una solución superviviente** — cuyo ⊇ es la ley de conservación, ya demostrada.

- [v61: la simetría vuelve — vale justo donde el lector trabaja (Claude)](./verificacion_inseguridad_autor_v61.md) — con la lección de v60 (mirar el estado final, no el recorrido) se reabre una puerta que yo mismo había cerrado. En v54 medí la simetría de la propiedad sobre **todos** los estados —185 violaciones— y la di por refutada. **Las 185 están todas en estados parciales**: en la línea final hay **0 en 3.849 nodos** sobre 64 estados, tres semillas. Con ella, `threaded` **se da la vuelta**: **`owners_contain_chain`** — la tabla de owners de un nodo no tiene solo una entrada por paso, **contiene un camino entero** — y con `OOS`, **`chain_through_of_symmetric`**: ese camino **pasa por el nodo**. O sea, **bajo simetría todo nodo está en un camino completo que posee entero**; es `SupportedAt` salvo si esos nodos se poseen **entre sí**. El atajo de leer la asignación de los owners de un solo nodo **no vale, y por una buena razón**: la clique falla (18–31 %) porque —justo como midió v60— los owners de un nodo son las proyecciones de *todas* las soluciones que pasan por él, y dos de ellos pertenecen a soluciones distintas. Medido al final: cadena golosa co-poseída en 2.412 de 2.540 anclas, el resto con otra cadena, **0 zombis**.

- [v62: la simetría — qué se demuestra, qué no, y por qué (Claude)](./verificacion_inseguridad_autor_v62.md) — **no la he demostrado**, y el informe empieza diciéndolo. Lo que hay: primero, un intento de romperla — dos campañas más, más variables, **0 violaciones en 11.009 nodos** sobre cinco semillas. Y después la localización, que es lo que vale: de las cuatro operaciones de la máquina, **`addNode` crea la propiedad simétricamente** (el nodo nuevo se lleva todos los owners globales y todo nodo se lleva el nuevo — tu `all_previous_nodes_are_owners_of_me!`) y **`filterRequire` no la toca**; ambas **demostradas**. `cleanInvalid` no puede romperla —interseca todas las tablas con la misma lista— pero la contabilidad de que la barrida cambia los owners globales según elimina nodos queda **argumentada, no demostrada**. Y **`reviewNode` es la única que puede romperla**: interseca con la unión de los owners de *sus propios vecinos*, una cantidad por nodo. Así que las 185 violaciones de v54 solo pueden venir de las pasadas de coherencia, y la demostración pendiente tiene forma concreta: *en un punto fijo de longitud completa, las asimetrías que las pasadas introducen ya han desaparecido* — una propiedad del punto fijo, no un invariante arrastrable, igual que la exactitud de v60.

- [v63: la barrida queda demostrada, y la simetría resulta ser cosa de los extremos (Claude)](./verificacion_inseguridad_autor_v63.md) — cierra el cuarto punto que v62 dejó como argumento: **`OwnSymmetric_cleanInvalid`**, la barrida no puede romper la simetría, bajo `Ownership.NodesAreGowners` (solo quita de una tabla ids que *no son owners globales*, y mientras todo nodo lo sea, un nodo no sale nunca de ninguna tabla). Con él `NG_cleanInvalid`, `OwnSymmetric_of_ownersEq`, las inversiones de `node?`, y `not_gowner_invalid` — la mitad provable de la recuperación. Y después, midiendo el alcance de la hipótesis (modo nuevo `--gowscope`, cinco semillas, 76 estados finales, 64 pinchazos), el resultado que de verdad importa: el pinchazo degrada 64 nodos y **`cleanInvalid` se lleva los 64 dejando 0 violaciones**; **`reviewParents` mete 236** en una sola pasada; el punto fijo del review deja 51; y **el final de la lectura vuelve a 0**. Es decir: **la simetría está en los dos extremos de una lectura y no en el medio** — la misma forma que la exactitud de v60, y una advertencia concreta para la ruta de v61: `chain_through_of_symmetric` no se puede arrastrar paso a paso a lo largo de una lectura.

- [v64: el review simétrico — y tu lectura, medida al pie de la letra (Claude)](./verificacion_inseguridad_autor_v64.md) — cuando el review quita `q` de `owners(p)` afirma *ninguna solución pasa por los dos*, frase simétrica que la máquina solo apuntaba en un lado; el paso nuevo `symmetrize` apunta el otro, detrás de cada intersección. Con la lectura como la describió el autor (`pinOwners`: elegir `r` es asumir elegidos todos sus owners, en todos los pasos a la vez, y después limpiar). **Demostrado** sin hipótesis: la simetría es invariante de toda lectura (`OwnSymmetric_read`), y ni el review simétrico ni el pinchazo por owners pierden soluciones (`ChainSound_reviewSym`, `ChainSound_readStepSym`). **Medido**: mismos veredictos que el original en 100 fórmulas, 0 soluciones perdidas, 132 asimetrías del original frente a **0** de la simétrica (join incluido), 0 fallos de `PickValid` en 5.260 elecciones; y, al pie de la letra, **elegir `r` conserva exactamente `owners(r)`** — 0 de 195.167 nodos compatibles muertos en 6.244 elecciones. El muro queda con una frase concreta: *elegir un nodo no mata a ninguno de sus owners*; la consistencia de caminos (0 huecos en ~197.000 pares a longitud completa) y la condición de enlace (0) la sostienen, y la demostración tendrá que ser un punto fijo con tablas que encogen.

- [v65: `Woven` generalizado — el tejido (Claude)](./verificacion_inseguridad_autor_v65.md) — `Woven` pedía que todo miembro poseyera a todo miembro (una camarilla), y `owners(r)` no lo es. **`Fabric g S T`**: cada miembro conserva una *subtabla* `T p` de sus owners, simétrica, con entrada en cada paso y con cada entrada respaldada por un padre y un hijo del tejido; `Woven` es el caso `T p v := S v` (`Fabric_of_Woven`). **Demostrado**: el tejido sobrevive a las cuatro operaciones, al review original entero (`isValid_filterAll_of_Fabric`) y al simétrico con el pinchazo por owners (`isValid_readStepSym_of_Fabric`); y `isValid_readStepSym_of_FabricAt` — si `owners(r)` contiene un tejido que pasa por `r`, elegir `r` nunca atasca al lector. **Medido** (`--fabric`): el mayor tejido dentro de `owners(r)` es **todo `owners(r)`** en 4.888 de 4.888 elecciones (159.621 nodos), recortando solo el 0,35 % de las entradas. El muro pasa de ser dinámico (¿qué borra el review?) a **estático**: *`owners(r)` contiene un tejido* — y el candidato `owners(p) ∩ owners(r)` solo necesita perder un 0,35 % sin vaciar ningún paso.

- [v66: el muro, releído con tu definición de owners (Claude)](./verificacion_inseguridad_autor_v66.md) — la definición del autor (owners = nodos compatibles; los owners globales contienen todos los nodos) formalizada como `OwnersExactAt`, y **demostrado** que implica que elegir `r` no mata a ninguno de sus owners (`alive_readStepSym_of_OwnersExactAt`), vía el tejido de soluciones (`Fabric_sol`, `FabricAt_of_chain`). **Medido** (`--tableexact`): a longitud completa la máquina cumple la definición al pie de la letra — 0 de 395.149 entradas espurias, 0 nodos sin solución, 0 nodos fuera de los owners globales, también tras cada elección. **Corrección a v60**: los estados intermedios, medidos contra las cláusulas *vistas hasta ese paso* y no contra la fórmula completa, dan 0 nodos zombie en 188.435 y solo 18 entradas espurias en 4,36 millones, todas en un único estado — pares compatibles uno a uno que ninguna solución combina, el hueco pares/conjunto visto en la práctica, que la máquina cierra más tarde. Búsqueda dirigida desde ese estado (`--hunt`): 0 veredictos zombie. El muro queda como invariante de nodo para una inducción sobre la construcción, con su paso difícil identificado: añadir una cláusula.

- [v67: el invariante de nodo, y el filtro de cláusulas (Claude)](./verificacion_inseguridad_autor_v67.md) — el invariante de nodo (`SupportedS`: todo nodo de todo estado válido está en una cadena sólida) **demostrado en toda la construcción salvo el filtro de los pasos de cláusula**: `seed`, `join`, `addNode`, el review, los filtros de literal y fusión, y el de negación (`TopKey_reachable`: la cima de todo estado lleva el id de su clave). Queda una única hipótesis local, `ClauseStepExact`, y con ella **la máquina decide 3SAT** (`decides_of_ClauseStepExact`: satisfacible ⟺ la ejecución acaba con un estado válido). `owns_required` formaliza lo que el filtro sí garantiza: compatibilidad con cada literal *por separado*; el lema pide los tres *a la vez*, una propiedad de tipo Helly. Como todo lo demás es teorema y la máquina es polinómica, demostrar `ClauseStepExact` para toda fórmula equivale a P = NP. Intentos de romperlo: 640 inserciones dirigidas al hueco del caso 17 y Tseitin sobre K4, K3,3, prisma y cubo — 0 violaciones, 0 veredictos zombie.

- [v68: el filtro de cláusula, en piezas pequeñas (Claude)](./verificacion_inseguridad_autor_v68.md) — `ClauseStepExact` partido y demostrado salvo un núcleo. **Parte 1** (demostrada, `SupportedS_filterAll_of_OneReqStep`): un requisito cada vez — el paso de cláusula son tres elecciones de lector seguidas. **Parte 2**: casos de un paso — (a) el nodo está en el paso del requisito (`step_case_self`, vía `owns_required` + `OOS`), (b) la cadena ya lo cumple, (c) el paso ya estaba fijado (`step_case_pinned`), todos demostrados; (d) hay que cambiar un literal: `FlipCore`, el núcleo. `decides_of_FlipCore`: la máquina decide 3SAT a partir solo del núcleo. **Medido** (`--flipcases`, 593.472 instancias): los casos demostrados cubren el 76 %; el núcleo actúa en el 24 % y siempre encuentra el cambio (0 fallos). La división natural del núcleo en pares es **falsa** (las 18 entradas del caso 17): el núcleo es existencial, de tipo Helly, y equivale a que la comprobación local de la máquina sea un oráculo exacto de satisfacibilidad para consultas pequeñas.

- [v69: el caso 17 explicado, un arreglo, y la conservación por prefijo (Claude)](./verificacion_inseguridad_autor_v69.md) — las 18 entradas del caso 17 son pares x0=0 / x2=1: la cláusula 1 (`x5 ∨ ¬x2 ∨ x0`) los permitía vía x5=1, y la cláusula 4 fijó x5=0 tres pasos después; cada nodo conserva owners en el paso 14 pero **no comparten ninguno**, y el review nunca mira pares. **Arreglo**: la pasada del triángulo (`triClean`, consistencia de caminos dentro del filtro), demostrada sin pérdida de soluciones (`ChainSound_triClean`, `ChainSound_reviewTri`) y medida: 216 → **0** entradas espurias en 5,58 millones, veredictos idénticos — con ella la definición de owners del autor se cumple en *todo* estado, no solo al final. No cierra el caso general (huecos de tríos). **Conservación por prefijo, demostrada**: `chainSound_along_prefix` (rama) y `pureSteps_carries_prefix` (driver): tras `k` pasos, toda asignación que satisface lo visto hasta `k` está como cadena en el estado de su clave.

- [v70: el caso general — lo que se ha podido y lo que no (Claude)](./verificacion_inseguridad_autor_v70.md) — la preocupación de los tríos era real *en las tablas*: con el triángulo, 0 huecos de pares pero 38 de tríos en 905.506 muestreados (`--triples`), 7 de ellos genuinos entre valores del mapa y ausentes de toda solución vista. Apuntando cláusulas a esos 7 (`--insertgen`, 800 fórmulas): **0 nodos zombie, 0 veredictos equivocados** — al fijar dos valores, la máquina propaga por las filas de las cláusulas (que codifican sus tres literales) y alcanza la contradicción. **Demostrado** `satUpTo_of_chain`: toda cadena de un estado intermedio se descodifica en una asignación que satisface lo visto — la dirección contraria de la conservación por prefijo. El caso general no se ha demostrado: la propagación tiene alcance acotado y probar que basta para toda fórmula es P = NP; lo demostrable es un teorema para una clase, y lo buscable es paridad sobre expansores con una implementación más rápida.

- [v71: la clase de alcance acotado, y lo que la medición corrigió dos veces (Claude)](./verificacion_inseguridad_autor_v71.md) — el camino (1) de v70. La clase (`CnfHypergraph.lean`): el hipergrafo de restricciones de la **fórmula** (una arista por cláusula), α-acíclico y con rondas GYO ≤ `K`, exigido sobre **todos los prefijos** — porque la α-aciclicidad **no es hereditaria** (testigo en dos líneas del propio código) y la máquina solo ve prefijos. **Demostrado**: `gyoIter_gyoRounds` (el contador es la iteración, sin axiomas), `gyoIter_eq_nil_of_BoundedScope` (la medida acotada que no menciona `nVars`), **`BoundedScope_prefix`** (la clase es cerrada bajo prefijos, que es lo que la inducción sobre pasos de cláusula necesita), decidibilidad, y cuatro ejemplos `decide`: 12 variables con la misma `K` que 3 — la clase **no** es «fórmulas pequeñas» — y el triángulo fuera a cualquier `K`. Las cinco familias Tseitin (K4, K3,3, prisma, cubo, Petersen) quedan fuera con núcleo cíclico de una arista por vértice. **Medido** (`cnfmap --flipscope`, cinco semillas, 400 fórmulas): la reparación **variable a variable se atasca dentro de la clase** (484 de 47.663) — la «Pieza 1» del plan era falsa, refutada antes de escribir Lean; los casos dicen por qué (todas las cláusulas sobre el mismo ámbito: una sola arista, pero varias relaciones), y la unidad correcta es la **fila**, que es lo que la máquina mueve. Por filas quedan 25; los casos restantes son filas localmente válidas y globalmente muertas, que es lo que quita el **semi-join**. Por filas tras el reductor: **0 atascos en 47.663** dentro de la clase, **1.192 de 415.984** fuera y **607 de 111.547** en los controles. El procedimiento que aguanta es el algoritmo clásico para CSP α-acíclicos, y su correspondencia con la máquina es término a término (filas = nodos de cláusula, semi-joins = pasadas de `review`, cuya arco-consistencia ya está demostrada sin hipótesis). `FlipCore` para la clase **no** demostrado: falta formalizar el reductor, el teorema de no-retroceso y el puente de vuelta a la cadena. **Corregido por v74:** la correspondencia «término a término» con la máquina que este informe afirma no está demostrada — v71–v73 no toca `owners` ni la máquina en ninguna línea de código.

- [v72: el reductor, formalizado — sobre las filas del propio mapa (Claude)](./verificacion_inseguridad_autor_v72.md) — la primera de las tres piezas que v71 dejó nombradas. `CnfReducer.lean` construye el reductor de semi-joins en el modelo puro **sobre la codificación de filas de `CnfMap`** (índices `1..7`, `b1`/`b2`/`b3`), no sobre una representación nueva: la fila que el reductor conserva *es* el índice del nodo de cláusula, lo que ahorra una traducción en el puente pendiente. `rowOk` descarta las filas que fijan una variable dos veces de forma contradictoria — no es cosmético, `WF` pide pasos distintos y no variables distintas, así que `x ∨ ¬x ∨ y` es legítima. **Demostrado, todo sin hipótesis y sin aciclicidad**: la fila de una asignación se lee de vuelta como la asignación (`mem_rowPairs_rowOfAssign`) y dos filas de solución nunca se contradicen; **conservación** (`Carries_sweep`, `rowOfAssign_mem_reduce`) — el reductor nunca tira una fila que una solución usa, y el paso no necesita nada de la fórmula porque la pareja que hace falta es la fila que la *misma* solución usa en la otra relación; su corolario `reduce_ne_nil_of_sat` (una relación vacía es una prueba de insatisfacibilidad, nunca un artefacto); **terminación** (`totalRows_sweepWith_lt`, `sweep_reduce`), sin la cual la arco-consistencia quedaría apoyada en una hipótesis sin descargar; y **`arcConsistent_reduce`**, la premisa de BFMY, el mismo enunciado que `ArcConsistency.review_arcConsistent` ya da de las tablas de owners. **Medido** (`cnfmap --reducer`, que corre el reductor *del modelo*, cinco semillas, 1.000 fórmulas): 0 prefijos satisfacibles con una relación vaciada en 16.349 — la banda del teorema demostrado; y, dentro de la clase, **39 de 39 prefijos insatisfacibles detectados**, frente a 9 de 688 no detectados fuera. Control: cada Tseitin de paridad impar deja exactamente **un** prefijo sin detectar — la fórmula entera, porque la consistencia de arcos no ve la paridad, que es justo por lo que la pieza 2 necesita la aciclicidad. Quedan abiertas las piezas 2 (no-retroceso bajo `BoundedScope`) y 3 (el puente de vuelta a la cadena). **Corregido por v74:** «la fila que el reductor conserva *es* el índice del nodo de cláusula que la máquina mantiene vivo» es falso como afirmación demostrada — que los supervivientes coincidan es la inclusión `owners ⊆ support`, abierta desde v11.

- [v73: la pieza 2, partida — el caso base demostrado y la diana medida (Claude)](./verificacion_inseguridad_autor_v73.md) — **`NoBacktrack` no está demostrado**, y el informe empieza diciéndolo; lo que trae es la obligación partida. `CnfSelection.lean`: una *selección* es una fila por cláusula, y *concuerda* cuando sus filas nunca dan dos valores a una variable. **Demostrado**: `sat_of_agreeing` — una selección que concuerda es una solución, que es el contenido de Helly (`ZeroOneAll.helly`) en la moneda de las fórmulas, sin aciclicidad ni reductor; y **`satisfiable_iff_agreeing_in_reduce`** — el reductor no pierde nada, buscar dentro del punto fijo no es más débil que buscar en todas partes, así que lo que queda es una sola frase sin pasadas ni grafo (`NoBacktrack`), con `satisfiable_iff_nonempty_of_NoBacktrack` diciendo que entonces una relación vacía *es* la insatisfacibilidad. **Caso base demostrado**: `NoBacktrack_of_singletons` — donde el reductor deja una fila por relación, la arco-consistencia tiene que apuntar a esa fila; es línea por línea el `NoChoice` de v19 y el `pairwiseOwned_of_fullyPinned` de v40, y cubre el 20 % de los prefijos de la clase (275 de 1.368). **Medido, y corrigió la diana** (`cnfmap --pickstep`, cinco semillas, 600 fórmulas): el paso de *una* elección es la obligación equivocada — dentro de la clase aguanta hasta en su forma fuerte (5.690 de 5.690 elecciones con **toda** fila válida, la distinción `PickSome`/`PickValid` de v41), pero **también se cumple en Tseitin impar (736 de 736), que no tiene ni una solución**: no hay invariante local que arrastrar, la lección de v18 y v60 otra vez. La diana correcta es el **descenso completo**, y ahí sí discrimina: **dentro de la clase 1.368 descensos golosos y los 1.368 llegan a una selección**, frente a 603 fallos sobre prefijos satisfacibles fuera; y 0 éxitos sobre prefijos insatisfacibles en todas partes — la banda del teorema del caso base. Falta la inducción sobre las `K` rondas de `gyoIter_eq_nil_of_BoundedScope`, que es formalizar BFMY sin Mathlib y es la parte cara del camino. **Corregido por v74:** `NoBacktrack` no es lo único que falta para `FlipCore` — falta además el puente con la máquina, que es lo gordo.

- [v74: corrección — v71–v73 no toca la máquina (Claude)](./verificacion_inseguridad_autor_v74.md) — a pregunta del autor, comprobado sobre el repositorio: en los tres módulos de v71–v73 hay **cero líneas de código** que mencionen `owners`, `gowners`, `GPathM`, `PathNodeId`, `isValid`, `filterAll`, `review` o `ChainSound` (las cuatro apariciones son prosa), y **ningún teorema conecta `reduce` o `NoBacktrack` con `FlipCore` o `ClauseStepExact`**. Segundo hecho: las tres campañas nuevas (`--flipscope`, `--reducer`, `--pickstep`) **tampoco ejecutan la máquina** — las de v66–v70 llaman a `pureAdvance` veinte veces, las nuevas ninguna —, así que sus números describen el procedimiento de fórmulas y no el algoritmo. Retiradas las frases concretas que afirmaban correspondencia «término a término», que la fila conservada *es* el nodo superviviente de la máquina, y que `NoBacktrack` era lo único que faltaba para `FlipCore`; corregidos también los docstrings de los módulos, porque es lo que una sesión futura lee primero. El diagnóstico: la simplificación es grande porque el modelo **asume** lo difícil — que los supervivientes del semi-join y los de la poda de `owners` coincidan es exactamente `owners ⊆ support`, abierta desde v11 —, de modo que **esta ruta no rodea el muro, lo vuelve a encontrar en el puente**; y cerrar `NoBacktrack` sin el puente daría el resultado de 1983 sobre CSP α-acíclicos, mudo sobre el algoritmo. Los teoremas de v71–v73 siguen siendo válidos, con sus cierres limpios, pero hablan del reductor de fórmulas y no de la máquina. Ningún teorema retirado; lo retirado son afirmaciones sobre su alcance.

- [v75: la banda — tu máquina es **más fuerte** que el reductor (Claude)](./verificacion_inseguridad_autor_v75.md) — medida la banda que v74 dejó pendiente (`cnfmap --band`): en cada paso de cláusula de ejecuciones reales, tres conjuntos de filas — las que sobreviven en la máquina, las que deja `CnfReducer.reduce` sobre el prefijo visto, y la verdad por fuerza bruta. Dos campañas (cinco semillas 3–6 variables y cuatro más 5–8, estas últimas expresamente para intentar romper el resultado), **5.203 pasos de cláusula**: **0 filas que la máquina conserve y el reductor no**, sin una excepción; **0 filas espurias de la máquina** frente a **2.166 del reductor**; y ninguno de los dos pierde jamás una fila verdadera. Dentro de la clase los dos conjuntos coinciden exactamente (426 pasos). Mismo patrón en los controles Tseitin, e idéntico con la pasada del triángulo. **La lectura invierte el plan de v71–v73**: el reductor no es un modelo de la máquina sino una **cota superior más débil** que ella, y por eso es útil al revés — si (A) toda fila que la máquina conserva la conserva el reductor y (B) el reductor no guarda espurias dentro de la clase, entonces la máquina tampoco las guarda ahí. Ninguna de las dos está demostrada; (A) es el puente real y es donde vuelven los `owners`. Dicho con el alcance exacto que v74 exige: (A)+(B) darían exactitud **a nivel de fila/clave** (del sabor de `OwnersExactAt`), **no `ClauseStepExact`**, que pide que todo *nodo* esté en una cadena sonora. La vía para (A): dos filas solo chocan por las variables que comparten, y en esos pasos de literal la máquina tiene `L1` más las dos pasadas de coherencia — estrictamente más que un semi-join.

- [v76: cómo demostrar que el algoritmo decide 3SAT — el mapa completo (Claude)](./verificacion_inseguridad_autor_v76.md) — análisis, sin código nuevo, a petición del autor y centrado en su diseño: el algoritmo no busca *una* solución, construye el conjunto de **todos los certificados**. Eso son dos inclusiones: **⊇** (no perder ninguno) está **demostrada de punta a punta** (`chainSound_along`, `pureRun_full_state`, v50–v53), y **⊆** (no guardar nada espurio) es todo lo que queda — «sin zombis», `ClauseStepExact`— con `decides_of_ClauseStepExact` ya demostrado, así que su caída da el teorema como corolario. La traducción del diseño al vocabulario clásico, que no es un préstamo sino una relectura: variables = pasos, dominios = nodos (valores de variable en el bloque de literales, filas en el de cláusulas), **`owners` = tabla de soportes por pares, es decir 2-consistencia** (ya dicho en `ZeroOneAll.lean`, v20), `review` = su punto fijo, la pasada del triángulo = un nivel más. De ahí, por Freuder (consistencia fuerte de nivel k + anchura inducida < k ⟹ sin retroceso), tres consecuencias: ningún nivel fijo vale para toda fórmula — por eso ⊆ general **es** P=NP, ya en v14/v67; sí vale en anchura acotada, que es el objetivo realista; y **subir el nivel del filtro ensancha la clase pero no cierra el caso general**, que es la respuesta a la pregunta del autor sobre correcciones y mejoras del filtrado. El hueco medido por niveles: 0 en pares tras el triángulo (v69), **38 de 905.506 en tríos, 7 genuinos** (v70), y aun así 0 nodos zombie y 0 veredictos malos (v66, v70, v75) — la exactitud de tabla ya falla en tríos, pero la propagación por filas cierra la contradicción a tiempo; eso es margen medido, no teorema. Tres rutas con su precio, y el primer teorema concreto propuesto: enunciar el puente (A) de v75 **sobre `owners`**, con `L1`/`ReqFiltered` y las dos pasadas de coherencia en los pasos de literal como vía, advirtiendo que (A) daría exactitud a nivel de **clave** y que subir de clave a **nodo** es un escalón más, identificado desde v67.

- [v77: el tejido en los pasos de cláusula, y una ruta que no pasa por `PairwiseOwned` (Claude)](./verificacion_inseguridad_autor_v77.md) — medido donde v65 no había mirado: no en la lectura sino en los **pasos de cláusula**, que es donde el filtro estrecha las tablas. `cnfmap --fabclause`, para **cada nodo** de cada estado válido, el mayor tejido dentro de `owners(n)`: **124.246 nodos** (84.538 en fórmulas aleatorias de 3–6 variables, 39.708 en los controles Tseitin) y **ni una excepción** — conserva todos sus miembros, **cubre todos los pasos** y contiene al propio nodo; idéntico en la máquina original y en la simétrica. El recorte que sí ocurre es de **entradas**, nunca de miembros — lo que `Fabric` permite por diseño (conserva una *subtabla*) —, y aparece donde cabía esperar: 0 % en aleatorias pequeñas y en K4, 0,003 % con 4–6 variables, 0,9–2,0 % en K3,3 y prisma; es decir, un fenómeno de **ciclicidad**, el eje de v17/v70/v75. **Y el hallazgo de la sesión, verificado en las firmas**: el veredicto **no necesita `PairwiseOwned` ni `ClauseStepExact`** — `L7.satisfiable_of_inhabited` solo consume `Inhabited`, que v58 reduce a `PickSome` y v65 a que `owners(r)` contenga un tejido que pase por `r`, que es justo lo medido. Ruta propuesta en cinco piezas: P1 el tejido nace en `addNode` (v62 ya da la simetría de la creación), P2 lo preserva el review (**hecho**, v65 — pero **corregido por v78**: falta `join`), P3 lo preserva el filtro de cláusula (**la pieza nueva**, con la medición de hoy como evidencia), P4 el puente `FabricAt ⟹ PickSome` — con un desajuste real declarado: `PickSome` habla de `filterAll g [q.id]` y v65 de `readStepSym = reviewSym ∘ pinOwners` —, y P5 cerrar con `L7`. La ruta esquiva todos los callejones documentados (caminos v43, transitividad v40, clique v28) y va entera sobre `owners`/`review`/el lector. Límite declarado: si las cinco cerraran para toda fórmula, 3SAT estaría en P, así que se espera que **P3 necesite la hipótesis de clase** — la anchura acotada de v76, ahora sobre las estructuras propias.

- [v78: P1 cerrada — el tejido nace en `addNode` (Claude)](./verificacion_inseguridad_autor_v78.md) — `FabricAdd.lean`: v65 demostró que el tejido **sobrevive**, nunca de dónde **sale**. Ahora sale. **Demostrado**, los cuatro con cierre `[propext, Quot.sound]`: `Fabric_initSeed` (la semilla ya es un tejido, dos cláusulas vacías), **`Fabric_addNode`** (el tejido se extiende a través de `addNode`, con el nodo nuevo entrando relacionado con todos los miembros), `FabricAt_addNode_new` (ese tejido vive dentro de sus propios owners) y `isValid_readStepSym_addNode_new` (elegir el nodo nuevo nunca atasca al lector). **La cláusula que necesitaba una idea es `up`**: el nodo nuevo tiene a todos los miembros en su tabla y `up` exige que cada entrada la respalde un **padre suyo** que esté en la tabla; como sus padres son toda la línea anterior, el portador resulta ser el **testigo de soporte de la propia entrada, girado por `symm`** — la simetría es portante, no decorativa, y por eso valía la pena que v64 la hiciera teorema. Y lo que permite cumplir la cláusula es el diseño del autor: `all_previous_nodes_are_owners_of_me!` más los padres de la línea entera son justo lo que un tejido pide. **Corrección a v77**: `Fabric.lean` **no menciona `join`** ni una vez, así que «sobrevive a las cuatro operaciones» era inexacto — el libro mayor queda semilla ✅ (hoy), `addNode` ✅ (hoy), review y filtros ✅ (v65), **`join` abierto**. Siguiente por rentabilidad: `join`, que es contabilidad, y después P4, que decide si la rama conecta con el veredicto.

- [v79: `join` cerrado, y P3 dicha con precisión (Claude)](./verificacion_inseguridad_autor_v79.md) — el caso que v78 detectó que faltaba se cierra en **un solo lema**, y la razón explica por qué el tejido es el objeto correcto: **todas sus cláusulas son pertenencias o existencias, ninguna dice «y nada más», así que ningún crecimiento puede romperlo**. `Join.lean` ya tenía la abstracción exacta (`Grown`), de modo que **`Fabric_of_grown`** sirve para todo y da `Fabric_join_left` y `Fabric_join_right`. Libro mayor de la inducción: semilla ✅, `addNode` ✅ (v78), **`join` ✅ (hoy)**, review y filtros de literal ✅ (v65). **Y la letra pequeña que cambia P3**: `FOk_filterAll` (v65) lleva la hipótesis `∀ r ∈ reqs, ∀ p, S p → p.id.step = r.step → p.id = r` — el filtro conserva el tejido *solo si sus miembros ya concuerdan con los pines* —, y en un paso de cláusula eso **falla de entrada**, porque el tejido de `addNode` contiene todos los owners globales y por tanto los dos valores de un paso pinzado. Así que P3 no es «el filtro preserva el tejido» sino: **al pinzar hay que estrechar el tejido a los miembros que concuerdan con los tres requisitos, y la obligación es que el estrechado siga teniendo una entrada en cada paso** — que es literalmente el `CoreCovers` de v44, reencontrado por otra ruta treinta y cinco informes después, y exactamente lo que midió v77 (124.246 nodos, 0 fallos). Expectativa declarada: ahí entrará la hipótesis de clase, porque P3 para toda fórmula sería 3SAT en P.

---
*Created by Gemini (Antigravity/Jules) in collaboration with Deepseek, Claude (Sonnet 5), and the Author.*
