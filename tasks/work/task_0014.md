---
FULL_TASK_ID: 0014
TAREAS_RELACIONADAS: ["0013"]
IDENTIFICADOR_DE_TAREA: P1_002_PATH_NODE_ID
ESTADO: PENDIENTE
FECHA_DE_CREACION: 2025-12-28
FECHA_DE_FINALIZACION:
HORAS_ESTIMADAS: 3
HORAS_REALES: 0
DESCRIPCION: "Migrate the `PathNodeId` and `SetPathNodesId` types, and the associated `new_path_id`, `as_key`, and `to_string` functions from `utils/alias.jl` to Lean 4."
ENTREGABLES:
  - lean_project/AbsSat/Utils/Alias.lean
CRITERIOS_DE_ACEPTACION:
  - The `PathNodeId` structure is defined in `lean_project/AbsSat/Utils/Alias.lean`.
  - The `as_key` function for `PathNodeId` is implemented and returns the correct string format.
  - The `to_string` functions for `PathNodeId` and `SetPathNodesId` are implemented and verified.
  - The implementation is verified with formal proofs and tested with examples.
PRIORIDAD: ALTA
RESPONSABLE: Jules
---

### SHOULD DO

1. **Modify `lean_project/AbsSat/Utils/Alias.lean`**.
2. **Define `PathNodeId` structure** with fields `id: NodeId` and `parentId: Option NodeId`.
3. **Implement `new_path_id` function**.
4. **Implement `as_key` for `PathNodeId` and `Option NodeId`**.
5. **Implement `to_string` for `PathNodeId` and `SetPathNodesId`**.
6. **Add `Theorems` section** with proofs for the new functions.
7. **Add `Examples` section** to test the implementations.

### ACCEPTANCE CRITERIA

- [ ] `PathNodeId` structure is correctly defined.
- [ ] `new_path_id` creates a correct `PathNodeId`.
- [ ] `as_key` for `PathNodeId` returns "k<id.step>.<id.index>__k<parent.step>.<parent.index>" or "k<id.step>.<id.index>__root".
- [ ] `as_key` for `Option NodeId` returns "root" for `none`.
- [ ] `to_string` for `SetPathNodesId` returns a newline-separated string of keys.
- [ ] All functions are proven correct in the `Theorems` section.
- [ ] `lake build` compiles without errors.
