**TASK ID:** 0007

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Media

**DEPENDENCIES:** None

**SHOULD TARGET:**
Migrate the `get_value` and `case_to_bits` functions from `utils/exaustive_solver.jl` to a new Lean 4 file `lean_project/AbsSat/Utils/ExaustiveSolver.lean`. These are the core pure functions of the exhaustive solver.

**SHOULD DO:**
1. Create a new file `lean_project/AbsSat/Utils/ExaustiveSolver.lean`.
2. Implement the `case_to_bits` function in Lean, which converts an integer to its binary string representation.
3. Implement the `get_value` function in Lean, which retrieves a boolean value from a binary string based on a literal index.
4. Add theorems to formally verify the correctness of both functions.

**ACCEPTANCE CRITERIA:**
1. The `lean_project/AbsSat/Utils/ExaustiveSolver.lean` file is created.
2. The `case_to_bits` and `get_value` functions are implemented in Lean and produce the same results as their Julia counterparts.
3. Lean theorems are provided to prove the correctness of both functions.

**RESOURCES:**
- `docs/original_julia/src/utils/exaustive_solver.jl`

**POTENTIAL RISKS:**
- String manipulation and bitwise operations in Lean might require careful handling to match the Julia implementation.

**SCOPE:**
- This task only covers the migration of the `get_value` and `case_to_bits` functions. The `ExSolver` struct and its methods are excluded.
