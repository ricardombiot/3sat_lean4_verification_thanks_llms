**TASK ID:** 0009

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Media

**DEPENDENCIES:** 0008

**SHOULD TARGET:**
Migrate the `ExSolver` struct and its `new` and `init!` functions from `utils/exaustive_solver.jl` to `lean_project/AbsSat/Utils/ExaustiveSolver.lean`. This will create the main stateful object for the exhaustive solver.

**SHOULD DO:**
1. Define the `ExSolver` structure in Lean, including its mutable fields.
2. Implement the `new` function to create and initialize an `ExSolver` instance.
3. Implement the `init!` function, which reads the CNF file and sets up the solver's initial state.
4. The `init!` function will need a `read_cnf_file!` helper function, which should also be migrated.
5. The `read_cnf_file!` function will use the `read_or` function from task 0008.

**ACCEPTANCE CRITERIA:**
1. The `ExSolver` struct is defined in Lean.
2. The `new` and `init!` functions are implemented and correctly initialize the solver.
3. The `read_cnf_file!` function is implemented and correctly parses the CNF file into the `gates_or` field.
4. The code compiles without errors.

**RESOURCES:**
- `docs/original_julia/src/utils/exaustive_solver.jl`
- `lean_project/AbsSat/Utils/ExaustiveSolver.lean`

**POTENTIAL RISKS:**
- Migrating a mutable struct from Julia to Lean requires careful state management, likely using the `IO` monad and `IO.Ref`.
- File I/O for `read_cnf_file!` will need to be handled correctly in Lean.

**SCOPE:**
- This task covers the creation and initialization of the `ExSolver`. The `run!` and other methods will be handled in subsequent tasks.
