---
FULL_TASK_ID: 0013
TAREAS_RELACIONADAS: []
IDENTIFICADOR_DE_TAREA: P1_001_NODE_ID_AS_KEY
ESTADO: PENDIENTE
FECHA_DE_CREACION: 2025-12-28
FECHA_DE_FINALIZACION:
HORAS_ESTIMADAS: 2
HORAS_REALES: 0
DESCRIPCION: "Migrate the `NodeId` type alias, `as_key` function, and `SetNodesId` type from `utils/alias.jl` to Lean 4."
ENTREGABLES:
  - lean_project/AbsSat/Utils/Alias.lean
CRITERIOS_DE_ACEPTACION:
  - The `NodeId` structure is defined in `lean_project/AbsSat/Utils/Alias.lean`.
  - The `as_key` function for `NodeId` is implemented and returns a string in the format "k<step>.<index>".
  - The `SetNodesId` type alias is defined as `Std.HashSet NodeId`.
  - The implementation is verified with a formal proof in a `Theorems` section.
  - The implementation is tested with examples in an `Examples` section.
PRIORIDAD: ALTA
RESPONSABLE: Jules
---

### SHOULD DO

1. **Crear el fichero `lean_project/AbsSat/Utils/Alias.lean`**.
2. **Definir la estructura `NodeId`** con los campos `step: Int` e `index: Int`, derivando las instancias `BEq`, `Hashable`, y `Repr`.
3. **Implementar la función `as_key` para `NodeId`** que devuelva el formato `k<step>.<index>`.
4. **Definir el alias `SetNodesId`** como `abbrev SetNodesId := Std.HashSet NodeId`.
5. **Añadir una sección `Theorems`** con una prueba formal que verifique la correctitud de `as_key`.
6. **Añadir una sección `Examples`** con una función `run_tests` que compruebe la implementación.
7. **Importar el nuevo módulo** en `lean_project/AbsSat.lean`.

### ACCEPTANCE CRITERIA

- [ ] El fichero `lean_project/AbsSat/Utils/Alias.lean` existe y contiene el código migrado.
- [ ] La estructura `NodeId` está definida correctamente.
- [ ] La función `as_key` produce el string esperado. Por ejemplo, para `NodeId.mk 1 2` debe devolver `"k1.2"`.
- [ ] El alias `SetNodesId` está definido.
- [ ] Las pruebas en la sección `Examples` se ejecutan y validan la implementación.
- [ ] El nuevo módulo se importa correctamente en el proyecto principal y `lake build` compila sin errores.
