**TASK ID:** 0002

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Alta

**DEPENDENCIES:** None

**SHOULD TARGET:**
Migrate the `NodeId` and `PathNodeId` types and their `as_key` functions from `utils/alias.jl` to a new Lean 4 file `lean_project/AbsSat/Utils/Alias.lean`. This includes creating the necessary type definitions and implementing the `as_key` functions with their corresponding formal verification theorems.

**SHOULD DO:**
1. Create a new file `lean_project/AbsSat/Utils/Alias.lean`.
2. Define the `NodeId` and `PathNodeId` types in Lean, corresponding to the Julia `NamedTuple` definitions.
3. Implement the `as_key` function for `NodeId`.
4. Implement the `as_key` function for `PathNodeId`.
5. Implement the `as_key` function for `Option NodeId` (to handle `NodeIdOrNothing`).
6. Add theorems to formally verify the correctness of each `as_key` implementation.

**ACCEPTANCE CRITERIA:**
1. The `lean_project/AbsSat/Utils/Alias.lean` file is created.
2. The `NodeId` and `PathNodeId` types are defined and compile correctly.
3. The `as_key` functions for `NodeId`, `PathNodeId`, and `Option NodeId` are implemented and produce the same string output as their Julia counterparts.
4. Lean theorems are provided to prove the correctness of the `as_key` functions.

**RESOURCES:**
- `docs/original_julia/src/utils/alias.jl`

**POTENTIAL RISKS:**
- The translation of Julia's `NamedTuple` to a Lean structure might require careful type mapping.

**SCOPE:**
- This task only covers the migration of `NodeId`, `PathNodeId`, and their `as_key` functions. Other functions and types in `alias.jl` are excluded.
