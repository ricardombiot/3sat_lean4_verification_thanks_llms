**TASK ID:** 0004

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Media

**DEPENDENCIES:** 0002

**SHOULD TARGET:**
Migrate the `get_value` function from `utils/checker.jl` to a new Lean 4 file `lean_project/AbsSat/Utils/Checker.lean`. This includes implementing the function in Lean and providing formal verification for its behavior.

**SHOULD DO:**
1. Create a new file `lean_project/AbsSat/Utils/Checker.lean`.
2. Implement the `get_value` function in Lean, which retrieves a boolean value from a solution array based on a literal index.
3. Add theorems to formally verify the correctness of the `get_value` function, covering both positive and negative literals.

**ACCEPTANCE CRITERIA:**
1. The `lean_project/AbsSat/Utils/Checker.lean` file is created.
2. The `get_value` function is implemented in Lean and correctly handles positive and negative literals.
3. Lean theorems are provided to prove the correctness of the `get_value` function.

**RESOURCES:**
- `docs/original_julia/src/utils/checker.jl`

**POTENTIAL RISKS:**
- The 1-based indexing of the Julia code will need to be carefully translated to Lean's 0-based indexing for arrays.

**SCOPE:**
- This task only covers the migration of the `get_value` function. Other functions in `checker.jl` are excluded.
