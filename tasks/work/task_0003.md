**TASK ID:** 0003

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Alta

**DEPENDENCIES:** 0002

**SHOULD TARGET:**
Migrate the `new_path_id` and `to_string` functions from `utils/alias.jl` to `lean_project/AbsSat/Utils/Alias.lean`. This includes implementing the functions in Lean and providing formal verification for their behavior.

**SHOULD DO:**
1. Implement the `new_path_id` function in Lean, which creates a `PathNodeId` from a `NodeId` and an optional parent `NodeId`.
2. Implement the `to_string` function for `PathNodeId`.
3. Implement the `to_string` function for `SetPathNodesId`.
4. Add theorems to formally verify the correctness of each function.

**ACCEPTANCE CRITERIA:**
1. The `new_path_id` function is implemented in Lean and correctly constructs a `PathNodeId`.
2. The `to_string` functions for `PathNodeId` and `SetPathNodesId` are implemented and produce the same string output as their Julia counterparts.
3. Lean theorems are provided to prove the correctness of the implemented functions.

**RESOURCES:**
- `docs/original_julia/src/utils/alias.jl`
- `lean_project/AbsSat/Utils/Alias.lean`

**POTENTIAL RISKS:**
- The `SetPathNodesId` `to_string` implementation requires careful handling of set iteration and string concatenation in Lean.

**SCOPE:**
- This task only covers the migration of the `new_path_id` and `to_string` functions from `alias.jl`.
