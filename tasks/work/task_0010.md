**TASK ID:** 0010

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Media

**DEPENDENCIES:** 0009

**SHOULD TARGET:**
Migrate the `run!`, `check_case!`, and `register_as_solution!` functions from `utils/exaustive_solver.jl` to `lean_project/AbsSat/Utils/ExaustiveSolver.lean`. This will complete the implementation of the exhaustive solver.

**SHOULD DO:**
1. Implement the `check_case!` function, which verifies if the current case is a valid solution.
2. Implement the `register_as_solution!` function, which saves a valid solution to the solver's state.
3. Implement the `run!` function, which iterates through all possible cases, checks them, and registers the solutions.
4. The functions will need to interact with the `ExSolver` state and use the previously migrated helper functions.

**ACCEPTANCE CRITERIA:**
1. The `run!`, `check_case!`, and `register_as_solution!` functions are implemented in Lean.
2. The `run!` function correctly iterates through all cases and finds all valid solutions for a given CNF file.
3. The `list_solutions` field of the `ExSolver` is correctly populated with the results.
4. The code compiles and runs without errors.

**RESOURCES:**
- `docs/original_julia/src/utils/exaustive_solver.jl`
- `lean_project/AbsSat/Utils/ExaustiveSolver.lean`

**POTENTIAL RISKS:**
- The main loop in `run!` needs to be carefully implemented to avoid infinite loops or off-by-one errors.
- State management with `IO.Ref` will be critical for the correct functioning of these methods.

**SCOPE:**
- This task covers the core logic of the exhaustive solver.
