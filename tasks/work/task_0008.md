**TASK ID:** 0008

**ROLE:** Developer

**PHASE:** P1

**PRIORITY:** Media

**DEPENDENCIES:** 0007

**SHOULD TARGET:**
Migrate the `calc_or` and `read_or` functions from `utils/exaustive_solver.jl` to `lean_project/AbsSat/Utils/ExaustiveSolver.lean`. These functions handle the parsing and evaluation of a single OR clause.

**SHOULD DO:**
1. Implement the `read_or` function in Lean, which parses a line from a CNF file into an array of three integer literals.
2. Implement the `calc_or` function in Lean, which evaluates an OR clause given a binary string representation of the current case and the literals.
3. The `calc_or` function should use the `get_value` function from task 0007.
4. Add theorems to formally verify the correctness of both functions.

**ACCEPTANCE CRITERIA:**
1. The `read_or` and `calc_or` functions are implemented in Lean.
2. `read_or` correctly parses a line into three integer literals.
3. `calc_or` correctly evaluates the OR clause and returns a boolean.
4. Lean theorems are provided to prove the correctness of both functions.

**RESOURCES:**
- `docs/original_julia/src/utils/exaustive_solver.jl`
- `lean_project/AbsSat/Utils/ExaustiveSolver.lean`

**POTENTIAL RISKS:**
- String parsing for `read_or` might be complex.
- Error handling for invalid line formats needs to be considered.

**SCOPE:**
- This task only covers the migration of the `calc_or` and `read_or` functions.
